# PyTorch currently ships its own CUDA runtime in the wheel.
# This file validates the project inside a CUDA 13 container while using a tested CUDA-enabled PyTorch wheel.
--index-url https://download.pytorch.org/whl/cu124
--extra-index-url https://pypi.org/simple
torch==2.5.1
numpy==2.1.3
pandas==2.2.3
pytest==8.3.4
